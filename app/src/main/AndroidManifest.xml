<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissões necessárias -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AvaliacaoVeiculos"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.AvaliacaoVeiculos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".CadastroVeiculoActivity"
            android:label="@string/cadastro_veiculo"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".CadastroItemActivity"
            android:label="@string/cadastro_item"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".CadastroFornecedorActivity"
            android:label="@string/cadastro_fornecedor"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".NovaAvaliacaoActivity"
            android:label="@string/nova_avaliacao"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".ChecklistActivity"
            android:label="@string/checklist"
            android:parentActivityName=".NovaAvaliacaoActivity" />
            
        <activity
            android:name=".DetalhesAvaliacaoActivity"
            android:label="@string/detalhes_avaliacao"
            android:parentActivityName=".MainActivity" />
            
        <activity
            android:name=".CameraActivity"
            android:label="@string/camera"
            android:screenOrientation="portrait" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>

